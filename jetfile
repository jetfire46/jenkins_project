pipeline {
    agent any
    environment {
        NODEJS_USER = "root"
        NODEJS_PASSWORD = "admin"
        NODEJS_IP = "65.0.135.30"
        APP_NAME = "sample-node-project"
        DEPLOY_DIR = "/${NODEJS_USER}/${APP_NAME}"
        NODEJS_HOME = tool name: 'node16', type: 'jenkins.plugins.nodejs.tools.NodeJSInstallation'
        PATH = "${NODEJS_HOME}/bin:${env.PATH}"
    }

    stages {
        stage('Checkout') {
            steps {
                echo '📥 Checking out source code...'
                git branch: 'main', url: 'https://github.com/jetfire46/jenkins_project.git'
            }
        }

        stage('Install System Dependencies') {
            steps {
                script {
                    echo '🧩 Installing system dependencies if needed...'
                    sh '''
                    if [ -x "$(command -v apt-get)" ]; then
                        echo "Debian/Ubuntu detected"
                        apt-get update -y
                        apt-get install -y libatomic1 sshpass || echo "Already installed"
                    elif [ -x "$(command -v yum)" ]; then
                        echo "RHEL/CentOS detected"
                        yum install -y libatomic sshpass || echo "Already installed"
                    else
                        echo "⚠️ Unsupported package manager. Ensure libatomic1 and sshpass are installed."
                    fi
                    '''
                }
            }
        }

        stage('Install Dependencies') {
            steps {
                echo '📦 Installing Node.js dependencies...'
                sh 'npm install'
            }
        }

        stage('Build and Package') {
            steps {
                echo '🏗️ Building project...'
                sh 'npm run build || echo "No build script found"'
            }
        }

        stage('Deploy to Server') {
            steps {
                echo '🚀 Deploying to remote server...'
                sh """
                sshpass -p '${NODEJS_PASSWORD}' scp -r ${WORKSPACE}/ ${NODEJS_USER}@${NODEJS_IP}:${DEPLOY_DIR}/
                sshpass -p '${NODEJS_PASSWORD}' ssh -o StrictHostKeyChecking=no ${NODEJS_USER}@${NODEJS_IP} '
                    cd ${DEPLOY_DIR} &&
                    pm2 stop ${APP_NAME} || true &&
                    pm2 start app.js --name ${APP_NAME}
                '
                """
            }
        }
    }

    post {
        success {
            echo '✅ Node.js Build and Deployment Successful'
        }
        failure {
            echo '❌ Build or Deployment Failed'
        }
    }
}
